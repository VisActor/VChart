```typescript
interface SunburstLabelConfig {
  /**
   * 标签对齐方式
   * @default 'center'
   */
  align?: 'center' | 'start' | 'end';

  /**
   * 标签旋转方式
   * @default 'radial'
   */
  rotate?: 'radial' | 'tangential';

  /**
   * 标签偏移距离
   * @default 0
   */
  offset?: number;
}
```
