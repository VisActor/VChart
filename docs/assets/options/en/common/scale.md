{{ target: common-scale }}

<!-- IVisualSpecScale -->

## scales(Array)

Global scales of the chart. Accepts an array of scale configurations.

{{ use: common-visual-spec-scale(
    prefix = '##',
    includeId = true
) }}