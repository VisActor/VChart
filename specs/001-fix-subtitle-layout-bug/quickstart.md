# Quickstart

## Usage

You can now configure a title component with only a subtitle:

```javascript
const spec = {
  type: 'bar',
  data: [ ... ],
  title: {
    visible: true,
    subtext: 'My Subtitle',
    // text is optional and can be omitted
  }
};
```
