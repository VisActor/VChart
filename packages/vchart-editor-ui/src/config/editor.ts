import type { ITitleEditorComponentConfig } from '../typings/config';

const titleDefaultConfig: ITitleEditorComponentConfig = {
  label: '图标标题',
  entries: [
    { key: 'fontSize', label: '字号' },
    { key: 'fontFamily', label: '字体' },
    { key: 'fontStyle', label: '样式' }
  ]
};
