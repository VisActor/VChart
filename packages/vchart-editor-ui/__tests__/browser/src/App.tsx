import './App.css';
import { Title } from '../../../src';

function App() {
  return (
    <div>
      <p>VChart Editor UI</p>
      <Title spec={{}} />
      {/* <Title spec={{}} entries={[{ key: 'fontSize' }]} /> */}
    </div>
  );
}

export default App;
