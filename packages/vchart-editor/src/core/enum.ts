export enum EditorActiveTool {
  line = 'line',
  text = 'text',
  data = 'data',
  chart = 'chart',
  none = 'none'
}

export enum EditorActionMode {
  addTool = 'add-tool',
  editData = 'edit-data',
  changeChart = 'change-chart',
  boxSelectionSelecting = 'box-selection-selecting', // 框选进行中
  none = 'none'
}
