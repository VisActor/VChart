<view tt:if="{{ spec1 }}" class="container">
  <vchart
    canvas-id="chart1"
    spec="{{ spec1 }}"
    styles="{{ styles }}"
  />
   <vchart
    canvas-id="chart2"
    spec="{{ spec2 }}"
    styles="{{ styles }}"
  />
</view>
